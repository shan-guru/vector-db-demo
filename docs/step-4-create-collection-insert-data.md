# Step 4: Create Collection & Insert Data

## Create a Collection

* Define a schema with:

  * Primary ID
  * Vector field (embeddings)
  * Optional metadata (text, source, etc.)

## Insert Records

* Convert documents into embeddings
* Insert vectors into Milvus
* Build an index for efficient similarity search

This step demonstrates **basic data ingestion** into Milvus.
